# Create a program that calculates the area of a rectangle (length × width) using variables and arithmetic operations.
length = int(input("Enter the length: "))
width = int(input("Enter the width: "))
result = length * width
print("The area of", length, "and", width, "is:", result)